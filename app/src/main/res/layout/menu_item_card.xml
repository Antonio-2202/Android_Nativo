<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:card_view="http://schemas.android.com/apk/res-auto"
    android:id="@+id/menu_item"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    card_view:cardCornerRadius="@dimen/lateral_margin_small"
    card_view:cardElevation="@dimen/lateral_margin_min"
    android:layout_margin="@dimen/lateral_margin_small"
    card_view:cardBackgroundColor="@color/login_field">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="@dimen/lateral_margin_default">

        <ImageView
            android:id="@+id/card_image"
            android:layout_width="80dp"
            android:layout_height="80dp"
            android:src="@drawable/ic_lock"
            android:layout_gravity="center"
            android:contentDescription="Menu Item Image" />

        <TextView
            android:id="@+id/card_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text=""
            android:textSize="@dimen/font_big"
            android:textColor="@color/white"
            android:layout_marginTop="@dimen/lateral_margin_small"/>

    </LinearLayout>

</androidx.cardview.widget.CardView>